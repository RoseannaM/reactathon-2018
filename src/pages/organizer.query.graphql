query event($id: ID!) {
  event(id: $id) {
    id
    title
    stream
    requests {
      cameraSession {
        id
        accessToken
      }
      screenSession {
        id
        accessToken
      }
      user {
        id
      }
    }
    session {
      id
      accessToken
    }
  }
}
