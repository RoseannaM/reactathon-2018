query event($id: ID!) {
  event(id: $id) {
    id
    title
    session {
      id
      stream
      accessToken
      requests {
        session {
          id
          accessToken
        }
        user {
          id
        }
      }
    }
  }
}
