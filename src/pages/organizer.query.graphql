query event($id: ID!) {
  event(id: $id) {
    id
    title
    stream
    requests {
      session {
        id
        accessToken
      }
      user {
        id
      }
    }
    session {
      id
      accessToken
    }
  }
}
